.venv/
__pycache__/
*.py[cod]

build/
dist/

# Runtime binaries are packaged into release assets. Some upstream FFmpeg
# binaries exceed GitHub's per-file limit, so keep them out of git.
bin/

.DS_Store
